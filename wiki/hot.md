---
type: hot-cache
updated: 2026-06-19
owner: vault
---

# gunner-brain — System Hot Cache

System-wide working memory for the whole team. Read this first at the start of any
session. For app-specific detail, see your own section's `hot.md`
(`tyler/hot.md`, `gunnerteam/hot.md`, `colin/hot.md`, `leo/hot.md`, `doug/hot.md`).

> This file is **manually curated**. It is not auto-generated by `/save` or `/lint`.
> Keep it short — only facts that more than one person needs.

---

## Tyler

> Tyler only writes to this block. Leo: do not edit.

- **Lambda:** v312 live (`gunnerteam-dev-api`, alias `live`, us-east-2)
- **iOS:** BUILD SUCCEEDED through cc-1801/cc-1128
- **Active:** RDS Proxy de-pin (cc-1628), points webhook HMAC fix (cc-1626), P&L push live (cc-1622)
- **Pending:** Colin points smoke-test; Colin P&L line-item confirm; employee notice HR/legal/IT sign-off

---

## Leo

> Leo only writes to this block. Tyler: do not edit.

_Leo: add your current state here — active projects, pending items, latest deploy._

---


## System State

| App | Owner | Status |
|---|---|---|
| GunnerTeam iOS + API | Tyler | Active. Lambda alias `live` (`gunnerteam-dev-api`, us-east-2, behind RDS Proxy). See [[tyler/hot]] for current version before deploying. |
| GunnerCam | Colin | Active. Multi-tenant AWS / Next.js field-operations app. |
| gunner-ops | Leo | Active. Job-lifecycle CRM replacing Monday.com. |
| Lead Finder / Review Engine / Content Creator / WP Local Page Template | Doug | Active. Standalone apps. |

- Update the table above only when an app's high-level status changes (new app, deprecation, major cutover).

## Recent Cross-Team Changes

**2026-06-15 — Tyler** — New process rule: git is the source of truth for GunnerTeam. No direct Lambda patches from anyone (Tyler, Colin, contractors). All changes via PR → reviewed merge → deploy from `main`. External contractors (Colin/Project Hub): do not patch GunnerTeam's Lambda or AWS resources directly. See [[gunnerteam/git-source-of-truth-policy]].

**2026-06-19 — Tyler** — `LOCATION_PING_FORWARD` flag ON and live. Location pings now forward to Colin (GunnerCam). Colin service key wired to `GET /time/location-compliance` with dual-auth. Both sides active.

Add an entry here whenever a change in your app affects another team member's app —
a schema change, an API contract change, a shared integration behavior change, etc.
Use newest-first. Each entry: date, who, what changed, who is affected. Then commit
and push so everyone picks it up on their next pull.

## Active Integration Points

Known places where apps talk to each other. If you change either side of one of these,
record it under **Recent Cross-Team Changes** and update the relevant
`shared/api-contracts/` file.

- **GunnerTeam iOS ↔ GunnerCam** — CompanyCam API integration: customer photos and
  webhooks. GunnerTeam consumes CompanyCam (`POST /projects/:id/photos`, photo records
  into `gt_customer_photos`) and receives CompanyCam webhooks. Colin's GunnerCam is the
  other side of the photo/field-ops flow.
- **GunnerTeam iOS ↔ gunner-ops** — job data flows between the iOS app/API and Leo's
  job-lifecycle CRM.
- **Doug's apps (Lead Finder, Review Engine, Content Creator, WP Local Page Template)**
  — standalone. No live cross-app integration points today; note any new ones here.

## Open Questions Across Apps

_Empty._

Add cross-team open questions here as they arise (anything blocking or affecting more
than one app). Resolve and remove, or promote to a `shared/decisions/` ADR.

## How to Update

Update this file when you make a change that affects another team member's app — a new
or changed integration point, a shared contract change, or a system-wide status change.
Commit and push so everyone gets it on their next pull. Keep your app's day-to-day
detail in your own section's `hot.md`, not here.
